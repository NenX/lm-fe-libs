import { BaseTableOld } from "@lm_fe/components_m";
export default class ProductsTable extends BaseTableOld {
    // renderOtherActions = () => {
    //     return <div>

    //         <span>快捷入口:</span>
    //         <PatientAutoComplete style={{ width: 120 }} onChange={(a, b) => {
    //             const pregnancy = b as unknown as IMchc_Pregnancy
    //             // @ts-ignore
    //             const { history } = this.props;
    //             console.log('ppp', pregnancy, history)
    //         }} />
    //     </div>
    // };
    // handleCheck = (rowData: any) => () => {
    //     const { id } = rowData;
    //     // @ts-ignore
    //     const { history } = this.props;
    //     history.push(`/prenatal-visit/pregnancy/check?id=${id}`);
    // };



    // handleEdit = (rowData: any) => () => {
    //     const { id } = rowData;
    //     // @ts-ignore
    //     const { history } = this.props;
    //     history.push(`/prenatal-visit/pregnancy/nurse-end?id=${id}`);
    // };
}
